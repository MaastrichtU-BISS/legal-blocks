{
  "name": "legal-blocks",
  "private": true,
  "engines": {
    "node": "^22.19.0 || ^24.11.0 || >=26.0.0"
  },
  "type": "module",
  "workspaces": [
    "packages/*",
    "layers/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --build --force"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^26.3.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.0"
  }
}
